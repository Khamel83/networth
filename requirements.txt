# Vercel serverless functions - using Supabase REST API
# NO supabase Python package - using httpx for lightweight HTTP calls instead
# NO Pillow - image processing moved to frontend (Canvas API)
httpx>=0.24.0
resend>=2.0.0
sentry-sdk>=1.40.0
