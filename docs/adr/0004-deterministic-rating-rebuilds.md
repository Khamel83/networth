---
status: accepted
---

# Deterministic Rating Rebuilds

Ratings will be rebuilt from canonical valid match records rather than adjusted by an untracked series of side effects. A corrected score replaces the prior result in the rebuild input, and repeating the same rebuild produces the same Ratings; this prevents duplicate submissions and administrative corrections from drifting the competition model.
